{
  "name": "ire-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Insurance Reasoning Engine (IRE) - Enterprise Healthcare SaaS Architecture",
  "scripts": {
    "build:packages": "pnpm --filter \"./packages/*\" run build",
    "build:services": "pnpm --filter \"./services/*\" run build && pnpm --filter \"./gateway/*\" run build",
    "build": "pnpm build:packages && pnpm build:services",
    "test": "pnpm -r run test",
    "dev": "docker-compose up --build",
    "services:up": "docker-compose up -d",
    "services:down": "docker-compose down"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
